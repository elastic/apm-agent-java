# Having difficulties keeping the individual classes run sequentially while having test methods run concurrently
# when running tests in IntelliJ. Keeping this disabled as a result, but useful to enable to evaluate handling
# concurrent requests.

junit.jupiter.execution.parallel.enabled = false
junit.jupiter.execution.parallel.mode.default = same_thread
junit.jupiter.execution.parallel.mode.classes.default = same_thread
