name: "Issue Labeler"
on:
  workflow_dispatch: ~
  issues:
    types: [opened]
  pull_request_target:
    types: [opened]

permissions:
  contents: read
  issues: write
  pull-requests: write

jobs:
  triage:
    runs-on: ubuntu-latest
    steps:
#    - name: Add agent-java label
#      uses: actions/github-script@v7
#      with:
#        script: |
#          github.rest.issues.addLabels({
#            issue_number: context.issue.number,
#            owner: context.repo.owner,
#            repo: context.repo.repo,
#            labels: ["agent-java"]
#          })

    - name: Get token
      id: get_token
      uses: tibdex/github-app-token@3beb63f4bd073e61482598c45c71c1019b59b73a # v2.1.0
      with:
        app_id: ${{ secrets.OBS_AUTOMATION_APP_ID }}
        private_key: ${{ secrets.OBS_AUTOMATION_APP_PEM }}
        permissions: >-
          {
            "members": "read",
            "organization_projects": "write",
            "issues": "read"
          }

    - name: Check team membership for user
      uses: elastic/get-user-teams-membership@1.1.0
      id: checkUserMember
      with:
        username: ${{ github.actor }}
        team: 'apm'
        usernamesToExclude: |
          apmmachine
          dependabot
          dependabot[bot]
        GITHUB_TOKEN: ${{ steps.get_token.outputs.token }}
    - name: Show team membership
      run: |
        echo "::debug::isTeamMember: ${{ steps.checkUserMember.outputs.isTeamMember }}"
        echo "::debug::isExcluded: ${{ steps.checkUserMember.outputs.isExcluded }}"

    - name: Add community and triage labels
      if: steps.checkUserMember.outputs.isTeamMember != 'true' && steps.checkUserMember.outputs.isExcluded != 'true'
      uses: AlexanderWert/issue-labeler@32be4a3c3d8f009c2741af471994337c34b4cb6f  # v2.3
      with:
        repo-token: "${{ secrets.GITHUB_TOKEN }}"
        configuration-path: .github/community-label.yml
        enable-versioned-regex: 0

    - name: Assign new internal pull requests to project
      if: (steps.checkUserMember.outputs.isTeamMember == 'true' || steps.checkUserMember.outputs.isExcluded == 'true')
      # && github.event_name == 'pull_request'
      run: |
        item_id=$(gh project item-add ${PROJECT_NUMBER_ID} --url ${PULL_REQUEST_URL} --owner elastic --format json --jq .id)
        gh project item-edit --id "${item_id}" --project-id "${PROJECT_ID}" --field-id "${STATUS_FIELD_ID}" --single-select-option-id "${STATUS_FIELD_VALUE_ID}"
        gh project item-edit --id "${item_id}" --project-id "${PROJECT_ID}" --field-id "${AGENT_FIELD_ID}" --single-select-option-id "${AGENT_FIELD_VALUE_ID}"
      env:
        MY_GITHUB_TOKEN: ${{ steps.get_token.outputs.token }}
        PROJECT_ID: 'PVT_kwDOAGc3Zs4Awt-_'
        PROJECT_NUMBER_ID: '1829'
        #PULL_REQUEST_URL: "https://github.com/elastic/apm-agent-java/pull/${{ github.event.pull_request.number }}"
        PULL_REQUEST_URL: "https://github.com/elastic/apm-agent-java/pull/4037"

        # Status: PVTSSF_lADOAGc3Zs4Awt-_zgm8e9E
        STATUS_FIELD_ID: 'PVTSSF_lADOAGc3Zs4Awt-_zgm8e9E'
        # In Progress: 47fc9ee4
        STATUS_FIELD_VALUE_ID: '47fc9ee4'

        # Agent: PVTSSF_lADOAGc3Zs4Awt-_zgnoTPA
        AGENT_FIELD_ID: 'PVTSSF_lADOAGc3Zs4Awt-_zgnoTPA'
        # java: 1cf9f1dc
        # dotnet: 23d446ac
        # python: c4f7a785
        # nodejs: 294a39a3
        # php: a076bea7
        # android: c897738c
        # ios: df64a860
        # rum: 92db652b
        # golang: 1bd45efd
        AGENT_FIELD_VALUE_ID: '1cf9f1dc'

