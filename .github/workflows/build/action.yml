---
name: common build tasks
inputs:
  command:
    description: 'Maven command'
    required: true
  shell:
    description: 'Default shell'
    default: 'bash'
    required: false
  java-version:
    description: 'Java version'
    required: true
  distribution:
    description: 'Java distribution'
    required: true

runs:
  using: "composite"
  steps:
    - name: Set up JDK ${{ inputs.java-version }}
      uses: actions/setup-java@v3
      with:
        java-version: ${{ inputs.java-version }}
        distribution: ${{ inputs.distribution }}
        cache: 'maven'

    - name: Fetch dependencies with a retry (1/2)
      run: ./mvnw clean install -DskipTests=true -Dmaven.javadoc.skip=true
      id: fetch-deps
      continue-on-error: true
      shell: ${{ inputs.shell }}

    - name: Fetch dependencies with a retry (2/2)
      run: ./mvnw clean install -DskipTests=true -Dmaven.javadoc.skip=true
      if: steps.fetch-deps.outcome == 'failure'
      shell: ${{ inputs.shell }}

    - run: ${{ inputs.command }}
      shell: ${{ inputs.shell }}
